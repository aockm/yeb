<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tockm.server.mapper.EmployeeRemoveMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.tockm.server.pojo.EmployeeRemove">
        <id column="id" property="id" />
        <result column="eid" property="eid" />
        <result column="after_depId" property="afterDepid" />
        <result column="after_jobId" property="afterJobid" />
        <result column="remove_date" property="removeDate" />
        <result column="reason" property="reason" />
        <result column="remark" property="remark" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, eid, after_depId, after_jobId, remove_date, reason, remark
    </sql>

</mapper>
